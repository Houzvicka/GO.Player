<Page
    x:Class="HBO.UWP.Player.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HBO.UWP.Player"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" DataContext="{Binding Main, Source={StaticResource Locator}}"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <ListBox x:Name="mListBox" Grid.Column="0" SelectionChanged="MListBox_OnSelectionChanged" />
        <ListBox x:Name="sListBox" Grid.Column="1" SelectionChanged="SListBox_OnSelectionChanged"  />
        <ListBox x:Name="tListBox" Grid.Column="2" SelectionChanged="TListBox_OnSelectionChanged" />
        <MediaElement x:Name="Element" Grid.Column="3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" AreTransportControlsEnabled="True"></MediaElement>
    </Grid>
</Page>
